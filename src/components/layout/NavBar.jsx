/**
 * @deprecated Import từ `components/Navbar` thay vì `components/layout`.
 * Giữ file này để không gãy import cũ — luôn render state khách (chưa đăng nhập).
 */
export { default } from "../Navbar/NavBar";

